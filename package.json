{
  "name": "dotto-typespec",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "dependencies": {
    "@typespec/compiler": "latest",
    "@typespec/http": "latest",
    "@typespec/openapi": "latest",
    "@typespec/openapi3": "latest",
    "@typespec/rest": "latest"
  },
  "devDependencies": {
    "@redocly/cli": "latest"
  },
  "scripts": {
    "build": "pnpm run compile && pnpm run docs:build",
    "compile": "rm -rf ./tsp-output/schema && tsp compile .",
    "watch": "tsp compile . --watch",
    "docs:build": "rm -rf ./dist && redocly build-docs app-bff-api -o ./dist/app-bff-api.html && redocly build-docs admin-bff-api -o ./dist/admin-bff-api.html && redocly build-docs announcement-api -o ./dist/announcement-api.html",
    "docs:preview": "redocly preview --project-dir=./tsp-output/schema/"
  }
}
